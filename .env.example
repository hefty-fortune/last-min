APP_ENV=local
APP_URL=http://localhost:8080

DB_DRIVER=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_NAME=lastmin
DB_USER=lastmin
DB_PASSWORD=lastmin
# Optional explicit DSN override. Leave empty to build DSN from DB_* values.
DB_DSN=

STRIPE_WEBHOOK_SECRET=dev-stripe-webhook-secret
