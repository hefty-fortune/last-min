services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "8080:8080"
    command: php -S 0.0.0.0:8080 -t . public_index_example.php

  # TODO(next prompt): enable FE container wiring once frontend Docker image/service is finalized.
  # frontend:
  #   build:
  #     context: ../frontend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - backend
